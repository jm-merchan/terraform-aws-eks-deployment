> ⚠️ **AI-Generated Deployment**
> Generated by **Bob**, an AI assistant powered by IBM watsonx.
> Review all code carefully before deploying to production.

# eks-dev-test

Deploys an EKS cluster in `eu-central-1` (dev environment) with a public-facing
NGINX test application to validate the cluster is working end-to-end.

## What gets created

| Resource | Details |
|---|---|
| VPC | `10.0.0.0/16`, 3 AZs, 1 shared NAT Gateway |
| EKS cluster | Kubernetes 1.33, private API endpoint, KMS encrypted |
| Node group | 1–3 × `t3.medium` (ON_DEMAND) |
| NGINX deployment | 2 replicas, `public.ecr.aws/nginx/nginx:1.27-alpine` |
| NLB (internet-facing) | Port 80 → NGINX pods |

## Mandatory tags

| Tag | Value |
|---|---|
| Environment | dev |
| Owner | jose-merchan |
| CostCenter | sandbox |
| Project | eks-dev-test |

## Deployment

```bash
cd deployments/eks-dev-test
terraform init          # uploads config to HCP Terraform workspace
```

Then trigger a plan/apply via HCP Terraform UI or:
```bash
aws eks update-kubeconfig --region eu-central-1 --name eks-dev-test-dev
kubectl get svc -n test-app   # shows the NLB hostname
```

## Validation

Once the NLB is provisioned (2–3 min after apply), open:
```
http://<nginx_load_balancer_hostname>
```
You should see the EKS Dev Test welcome page.
